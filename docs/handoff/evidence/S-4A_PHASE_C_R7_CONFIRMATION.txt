S-4a confirmatory code review after round-7 C / C' PASS

implementation base: be33e4716c70334100aada23c555ac75a0b0dbb7
implementation head: 3b7b64fe4517d3737a89868f4672d2f0236010a0
role: confirmatory review of production deltas after 88fda3e (R4–R6), not a replacement of R7 C/C'
reviewer: Cursor Grok 4.6 / SpaceXAI+Cursor (new session)

This session read production sources at 3b7b64f, git history 88fda3e..3b7b64f, frozen HANDOFF §2/§3, and the R7 C/C' review texts only after forming a source verdict. It does not replace `S-4A_PHASE_C_R7_REVIEW.txt` / `S-4A_PHASE_C_PRIME_R7_REVIEW.txt`.

R7 evidence files listed in the HANDOFF manifest (complete diff, test XML, Unity log, contract-audit, machine-checks) are not present in this worktree. They remain untracked as in earlier rounds. Unity / pwsh are absent here, so tests were not re-run. The 681/681 claim is taken from the recorded R7 reviews and HANDOFF, not recomputed.

Source verdict on R4–R6 closures
--------------------------------
- CreateReservedResource writes CreatingReserved to the journal before CreateAsset, faults at ResourceReservedBeforeInitialization, then sets identity and Initialized + CheckpointAsset. Empty-stub recovery is possible; non-zero payload during CreatingReserved fails closed.
- BeginGeneratedMutation still clears scene/node/resource fingerprints before each Generate. Initialized + empty fingerprint still accepts stub (payload 0) or generated leaf payload. Recorded fingerprint mismatch remains fail-closed.
- VerifyGraphMembership scans project SceneGraphEdges before destructive rollback. A pending node referenced by a graph other than journal.graphGuid throws. The recorded graph must be exactly one node membership, one child edge from the recorded parent, and zero parent edges.
- VariantPlayerBuild.EscapeJsonString emits named JSON escapes and `\uXXXX` for remaining C0 controls (`character < ' '`).
- Line counts: transaction 390, OwnershipProof 266, VariantPlayerBuild 192. Stop condition >400 not fired. No new public API / asmdef / SceneState / LoadType.

C' residuals (independent agreement)
------------------------------------
- F1 Medium: SessionCellCompanionLoadDriver logs non-OCE AddScene failures and does not UnloadScene. CP-10 text is the post-success parent-race predicate. Not a bounce to Phase B.
- F2 Low: Workspace open mid-failure is WW-4.
- F3 Low: CreatingReserved allows empty or matching identity with payload 0. Intended stub proof.

Findings: none at High. No Phase A / B return.

Disposition: agree with R7 C PASS and C' PASS. Merge remains human Phase D. Do not mark the PR ready from this session.
