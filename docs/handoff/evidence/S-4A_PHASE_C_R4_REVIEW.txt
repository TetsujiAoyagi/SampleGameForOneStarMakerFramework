S-4a Phase C review result (round 4)

implementation base: be33e4716c70334100aada23c555ac75a0b0dbb7
reviewed implementation head: 88fda3ef32f08ac1d0bf0f7f021b047c5b697b6a
compared against previous implementation head: 156cfd7dbd11f7f0d187ddbf1fd8c5943763c028
result: PASS (structure + round-3 closure). Merge acceptance remains held until a Phase C operator freezes test XML at this head and regenerates the R4 evidence / C' blind bundles.

reviewer: Cursor Grok 4.6 / SpaceXAI+Cursor (new session; Phase B of 88fda3e was Claude Opus 5 / Anthropic)

This review consumed the frozen Phase A snapshot, the 88fda3e production/test diff, and the round-3 FAIL required action. It did not consume the C' review as an input to the verdict. Unity tests were not executed: this Cloud Agent environment has no Unity.exe / pwsh, and the repository contract forbids launching Unity.exe here. 156cfd7's 672/672 XML is stale for this head.

Responsibility-map conformance: PASS
------------------------------------
- No new public/protected API, asmdef, package, SceneState, LoadType, or owner.
- WorldCompanionOwnershipProof remains the internal durability seam of the same Editor transaction owner. BeginGeneratedMutation is the missing half of RefreshGeneratedAssets, not a second responsibility.
- WorldCompanionCreationTransaction.cs is 385 lines (stop condition >400 not fired). OwnershipProof is 224 lines.
- Scene / node fingerprints were cleared with the resource one. That is the implementer's documented extra step, not a map change: RefreshGeneratedAssets already recaptured all three, and fingerprints include .meta bytes.

Round-3 P1 closure: PASS
------------------------
Required action was: leave fingerprints empty across Generate, use GUID + VerifyExpectedShape while empty, keep fail-closed once a fingerprint is recorded, and add a fault seam between Generate and the journal refresh.

Implemented:
- BeginGeneratedMutation saves empty scene/node/resource fingerprints before each Generate (Execute lines 90 and 97).
- GeneratedBeforeCheckpoint fires after the first Generate and before RefreshGeneratedAssets (line 92).
- VerifyOrCaptureAsset now: GUID mismatch fail-closed; empty fingerprint → VerifyExpectedShape + recapture; recorded fingerprint mismatch → Content ownership mismatch.
- Named test CrashBetweenGenerateAndCheckpoint_RollsBack_InsteadOfBlockingOnStaleFingerprint asserts resourceFingerprint is empty after the fault and RecoverPending rolls back.
- Parameterized recovery / transaction fixtures iterate the new enum value automatically.

The create-before-checkpoint path still works: empty GUID + empty fingerprint still enters the shape branch.

Round-2 external-mutation guarantee remains: LeaveFullyCreatedPending still faults at Regenerated (after the post-Generate refresh), so fingerprints are populated. Same-GUID Scene/Node/Resource/Addressables edits still throw before deletion.

C' P2 closure (observed, not used as an independent finding): PASS
-------------------------------------------------------------------
BuildVariantProfileSceneVariantTests SetUp stores EditorSceneVariantResolver and TearDown restores it. It no longer writes null.

Findings
--------
None at High or Medium.

Low / residual (not blocking):
- The second Generate (idempotence pass) calls BeginGeneratedMutation + Generate + Refresh but has no GeneratedBeforeCheckpoint seam. Production recovery is the same empty-fingerprint path. Only the first Generate window has an explicit named fault. Not a WW-10 hole.
- While fingerprints are empty, an external edit that still matches VerifyExpectedShape (for example a .name change) would be treated as owned and deleted. That window is the accepted cost of the required action. Once RefreshGeneratedAssets saves hashes, the round-2 fail-closed rule returns.
- Crash mid-Generate before SaveAssets leaves the previous on-disk stub, which the shape proof already accepts.

Machine checks performed in this session
----------------------------------------
- git diff --check 88fda3e: not re-run as a named script; the four changed Unity C# files keep #nullable enable.
- Unity record / Task.Delay / Thread.Sleep in the 156cfd7..88fda3e C# diff: 0.
- asmdef / Packages / SceneState / LoadType name-status against be33e47: 0.
- pwsh tools/contract-audit.ps1 and pwsh tools/run-tests.ps1: not executed (pwsh and Unity.exe absent).

Unreviewed / required before merge
----------------------------------
- Fresh `pwsh tools/run-tests.ps1` at 88fda3e. Expected: previous 672 plus the new named recovery case (parameterized fixture grows by one mutation point as well). Phase B estimated 674.
- R4 evidence bundle and C' blind bundle must be generated from the same instant, then C' must run in a new session on a model other than this Phase C and the 88fda3e Phase B, without this file.
- Addressables full build remains S-4b.
- 156cfd7 / R3 evidence / R3 C' PASS are not acceptance inputs for 88fda3e.

Disposition: do not return to Phase B. Do not reopen Phase A. Hold Phase D until test evidence and a blind C' at this head exist.
