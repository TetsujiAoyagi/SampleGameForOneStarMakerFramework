# U66 A2 independent package/test/rollback review

- reviewer: `/root/u66_a2_packages`
- model: GPT-5.6 Luna (OpenAI), separate context
- input: A1 snapshot plus base `3244f3635c6e18fffc1bbc40d3126e6d1eee009a` to implementation head `4263a33ee0d5e75b67e82e6260ff556f12fd1dce`; Phase C conclusions excluded
- verdict: exact package provenance and source/serialization repair evidence must be decided in A3; implementation can be retained if those deviations are explicit.

Recommendations:

1. Core 6.6 package updates are acceptable. Ads/collab-proxy/VisualScripting, Multiplayer Center, and tetgen should be rejected or deferred unless resolver provenance is available; if retained, require explicit human exception.
2. Git dependency URLs are not SHA-pinned. Either treat manifest plus tracked lock and hash verification as the restore contract, or change implementation separately.
3. Accept Addressables `m_DisableWriteTypeTree: 0` only with evidence it was generated by 6.6/Addressables migration and leaves semantics unchanged.
4. Accept the TMP compatibility repair only with the original compiler failure and minimal-repair evidence.
5. Raw XML/log must remain the hard gate because the wrapper itself does not fail on skips and can mask some process-exit distinctions. Wrapper changes are separate scope.
6. Keep the rollback isolation policy, but record that the 6.5 rollback and base behavior were not demonstrated and never delete the user-owned untracked pre-Phase-A document.
