<UserControl x:Class="DebugStudio.App.Features.Session.SessionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Padding="12"
                BorderBrush="#FF3A3A3A"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock FontWeight="SemiBold" Text="Session Listener" />

                <Grid Grid.Row="1" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock VerticalAlignment="Center"
                               Margin="0,0,8,0"
                               FontWeight="SemiBold"
                              Text="Listen URI" />
                    <TextBox Grid.Column="1"
                             Margin="0,0,8,0"
                             MinWidth="220"
                             Text="{Binding ServerUri, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2"
                            Margin="0,0,8,0"
                            Padding="14,6"
                            Command="{Binding ConnectCommand}"
                           Content="Start Listening" />
                    <Button Grid.Column="3"
                            Padding="14,6"
                            Command="{Binding DisconnectCommand}"
                           Content="Stop Listening" />
                </Grid>

                <Grid Grid.Row="2" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                       <TextBlock FontWeight="SemiBold" Text="Listener" />
                        <TextBlock Margin="0,8,0,0"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Text="{Binding ConnectionState, Mode=OneWay}" />
                        <TextBlock Foreground="#FF666666"
                                   TextWrapping="Wrap"
                                   Text="{Binding ConnectionDetail, Mode=OneWay}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock FontWeight="SemiBold" Text="Capabilities" />
                        <TextBlock Margin="0,8,0,0"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Text="{Binding CapabilityStatus, Mode=OneWay}" />
                        <TextBlock Foreground="#FF666666"
                                   TextWrapping="Wrap"
                                   Text="{Binding CapabilityDetail, Mode=OneWay}" />
                        <TextBlock Margin="0,6,0,0"
                                   Foreground="#FF666666"
                                   TextWrapping="Wrap"
                                   Text="{Binding NegotiatedCapabilitiesText, Mode=OneWay}" />
                        <TextBlock Margin="0,6,0,0"
                                   Foreground="#FF666666"
                                   TextWrapping="Wrap"
                                   Text="{Binding SessionIdentityText, Mode=OneWay}" />
                        <TextBlock Margin="0,6,0,0"
                                   Foreground="#FF666666"
                                   TextWrapping="Wrap"
                                   Text="{Binding LocalCapabilitiesText, Mode=OneWay}" />
                        <TextBlock Margin="0,4,0,0"
                                   Foreground="#FF666666"
                                   TextWrapping="Wrap"
                                   Text="{Binding RemoteCapabilitiesText, Mode=OneWay}" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Margin="0,12,0,0"
                Padding="12"
                BorderBrush="#FF3A3A3A"
                BorderThickness="1"
                CornerRadius="6">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Margin="0,0,0,8"
                           FontWeight="SemiBold"
                           Text="Recent Activity" />
                <ListBox ItemsSource="{Binding RecentActivity}"
                         FontFamily="Consolas"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" />
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
